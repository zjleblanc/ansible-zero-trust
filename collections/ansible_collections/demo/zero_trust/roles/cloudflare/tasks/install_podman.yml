---
- name: Assert tunnel token is available
  no_log: "{{ cloudflare_no_log | default(true) }}"
  ansible.builtin.assert:
    fail_msg: >-
      cloudflare_tunnel_token is required. Run create_tunnel first in the same play,
      or set cloudflare_tunnel_token explicitly.
    that:
      - cloudflare_tunnel_token is defined
      - cloudflare_tunnel_token | length > 0

- name: Check for rpm-ostree (immutable OS)
  become: true
  register: _cloudflare_ostree_boot
  ansible.builtin.stat:
    path: /run/ostree-booted

- name: Install podman
  when: not (_cloudflare_ostree_boot.stat.exists | default(false))
  become: true
  ansible.builtin.package:
    name: podman
    state: present

- name: Ensure rootless Podman user exists
  when: cloudflare_podman_rootless | bool
  become: true
  ansible.builtin.user:
    name: "{{ cloudflare_podman_user }}"
    state: present

- name: Look up rootless Podman user account details
  when: cloudflare_podman_rootless | bool
  ansible.builtin.getent:
    database: passwd
    key: "{{ cloudflare_podman_user }}"

- name: Check if lingering is enabled for rootless Podman user
  when: cloudflare_podman_rootless | bool
  become: true
  register: cloudflare_podman_linger_check
  ansible.builtin.stat:
    path: "/var/lib/systemd/linger/{{ cloudflare_podman_user }}"

- name: Enable lingering for rootless Podman user
  # No dedicated module wraps `loginctl enable-linger`; the stat check above keeps this
  # idempotent (only runs, and reports changed, the first time). Linger keeps the user's
  # systemd instance (and its rootless containers) running without an active login session.
  when:
    - cloudflare_podman_rootless | bool
    - not (cloudflare_podman_linger_check.stat.exists | default(false))
  become: true
  ansible.builtin.command:
    cmd: "loginctl enable-linger {{ cloudflare_podman_user }}"

- name: Set Podman privilege and path facts
  vars:
    _rootless: "{{ cloudflare_podman_rootless | bool }}"
  ansible.builtin.set_fact:
    # Always become, then drop to cloudflare_podman_user when rootless (become_user
    # is ignored when become is false). Use | bool — Jinja "{{ false }}" can stringify.
    _cloudflare_podman_become: true
    _cloudflare_podman_become_user: "{{ cloudflare_podman_user if (_rootless | bool) else '' }}"
    _cloudflare_podman_systemd_scope: "{{ 'user' if (_rootless | bool) else 'system' }}"
    _cloudflare_podman_quadlet_dir: >-
      {{ (ansible_facts.getent_passwd[cloudflare_podman_user][4] + '/.config/containers/systemd')
         if (_rootless | bool) else '/etc/containers/systemd' }}
    _cloudflare_podman_environment: >-
      {{ {'XDG_RUNTIME_DIR': '/run/user/' + ansible_facts.getent_passwd[cloudflare_podman_user][1]}
         if (_rootless | bool) else {} }}

- name: Create Podman network
  when: cloudflare_podman_network | length > 0
  become: "{{ _cloudflare_podman_become }}"
  become_user: "{{ _cloudflare_podman_become_user if (_cloudflare_podman_become_user | length > 0) else omit }}"
  environment: "{{ _cloudflare_podman_environment }}"
  containers.podman.podman_network:
    name: "{{ cloudflare_podman_network }}"
    state: present

- name: Pull cloudflared container image
  become: "{{ _cloudflare_podman_become }}"
  become_user: "{{ _cloudflare_podman_become_user if (_cloudflare_podman_become_user | length > 0) else omit }}"
  environment: "{{ _cloudflare_podman_environment }}"
  containers.podman.podman_image:
    name: "{{ cloudflare_container_image }}"
    state: present

- name: Store tunnel token as Podman secret
  become: "{{ _cloudflare_podman_become }}"
  become_user: "{{ _cloudflare_podman_become_user if (_cloudflare_podman_become_user | length > 0) else omit }}"
  no_log: "{{ cloudflare_no_log | default(true) }}"
  environment: "{{ _cloudflare_podman_environment }}"
  containers.podman.podman_secret:
    name: "{{ cloudflare_podman_secret_name }}"
    state: present
    data: "{{ cloudflare_tunnel_token }}"
    force: true

- name: Ensure Quadlet unit directory exists
  become: "{{ _cloudflare_podman_become }}"
  become_user: "{{ _cloudflare_podman_become_user if (_cloudflare_podman_become_user | length > 0) else omit }}"
  ansible.builtin.file:
    state: directory
    mode: "0755"
    path: "{{ _cloudflare_podman_quadlet_dir }}"

- name: Install cloudflared Quadlet unit
  become: "{{ _cloudflare_podman_become }}"
  become_user: "{{ _cloudflare_podman_become_user if (_cloudflare_podman_become_user | length > 0) else omit }}"
  register: cloudflare_quadlet
  ansible.builtin.template:
    dest: "{{ _cloudflare_podman_quadlet_dir }}/{{ cloudflare_container_name }}.container"
    mode: "0644"
    src: cloudflared.container.j2

- name: Reload systemd to pick up Quadlet unit
  become: "{{ _cloudflare_podman_become }}"
  become_user: "{{ _cloudflare_podman_become_user if (_cloudflare_podman_become_user | length > 0) else omit }}"
  environment: "{{ _cloudflare_podman_environment }}"
  ansible.builtin.systemd:
    daemon_reload: true
    scope: "{{ _cloudflare_podman_systemd_scope }}"

- name: Enable and start cloudflared Quadlet service
  become: "{{ _cloudflare_podman_become }}"
  become_user: "{{ _cloudflare_podman_become_user if (_cloudflare_podman_become_user | length > 0) else omit }}"
  environment: "{{ _cloudflare_podman_environment }}"
  ansible.builtin.systemd:
    name: "{{ cloudflare_container_name }}.service"
    scope: "{{ _cloudflare_podman_systemd_scope }}"
    state: "{{ 'restarted' if cloudflare_quadlet.changed else 'started' }}"
    enabled: true
